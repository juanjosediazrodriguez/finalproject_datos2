#ifndef MERGESORT_HPP
#define MERGESORT_HPP

#include "parser.hpp"
#include <vector>

// Ordena `a` por `tenure` descendente (estable).
void merge_sort_desc(std::vector<Solicitud>& a);

#endif // MERGESORT_HPP
