#ifndef BINARY_SEARCH_HPP
#define BINARY_SEARCH_HPP

#include "parser.hpp"
#include <vector>

// devuelve indice de primera solicitud
int binary_search_first_ge(const std::vector<Solicitud>& a, int k);

#endif 
